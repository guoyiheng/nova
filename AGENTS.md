> 使用中文和我对话

## 协作方式（重要）

- **每个任务做完 git 提交一次**，提交信息写清做了什么，方便我回溯。
- 任务按「简单到难」顺序做，代码质量要高。
- 内部实现逻辑不暴露给用户（如「待上传」「超出 N 字不传给模型」这类提示不写到前台）。
- 版本发布默认发布小版本，
  - 例如：v0.1.0 -> v0.1.1 -> v0.1.2
  - 只有在指定要发大版本时才发布，例如：v0.1.0 -> v0.2.0
- 更新日志收集版本间的提交日志，提取用户需要知道的更新内容即可，不要太多，挑选最重要的前3条